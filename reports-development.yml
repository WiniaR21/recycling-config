#Database spec
spring:
  datasource:
    driverClassName: org.postgresql.Driver
    url: jdbc:mysql://localhost:5432/reportsdb
    username: root
    password: root
  jpa:
    database: POSTGRESQL
    hibernate:
      ddl-auto: create-drop

#Actuator
management:
  endpoints:
    web:
      exposure:
        include: "*"
  health:
    readiness-state:
      enabled: true
    liveness-state:
      enabled: true
  endpoint:
    shutdown:
      enabled: true
    health:
      probes:
        enabled: true